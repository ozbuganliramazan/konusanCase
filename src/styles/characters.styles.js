import {StyleSheet} from 'react-native';
import {AppColors} from '../theme/colors';

const characterScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: AppColors.Black,
  },
});

export {characterScreenStyles};
